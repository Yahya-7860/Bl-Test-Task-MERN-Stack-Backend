const { amountRouter } = require("./amountRoute");
const { groupRouter } = require("./groupRoute");
const { userRouter } = require("./userRoute");

module.exports = { userRouter, groupRouter, amountRouter }